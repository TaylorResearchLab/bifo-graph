# bifo-graph runtime dependencies (Python 3.11).
#
# This pin set is shared with the sibling project bifo-spectral, which uses
# the same conda env (bifo-spectral). Keep these files in sync between repos.
#
# Update procedure:
#   1. conda activate bifo-spectral
#   2. pip install --upgrade <pkg>
#   3. python -c "import <pkg>; print(<pkg>.__version__)"
#   4. update the matching line below
#   5. mirror the update in the bifo-spectral repo

# Numerics
numpy==2.4.3
scipy==1.17.1
pandas==3.0.2

# Graph operations
networkx==3.6.1

# Config
pyyaml==6.0.3
pydantic==2.13.3

# Knowledge graph backend (Bolt protocol)
neo4j==6.1.0

# Plotting (used in optional analysis paths)
matplotlib==3.10.9
seaborn==0.13.2

# Statistics
statsmodels==0.14.6
scikit-learn==1.8.0
