# Fixture: bifo_mapping where one predicate entry is missing required
# fields (direction or classification). The loader should report which
# predicate is malformed.

version: 0.0.0-fixture

edge_resolution:
  predicate_to_flow:

    inverse_pathway_associated_with_gene:
      # Missing 'classification' field; only direction is set.
      direction: source_to_target
