# Fixture: bifo_mapping that classifies the referenced predicate with a
# value not in the known taxonomy. Cross-validation should fail with a
# helpful error mentioning the expected values.

version: 0.0.0-fixture

edge_resolution:
  predicate_to_flow:

    inverse_pathway_associated_with_gene:
      direction: source_to_target
      classification: invented_unknown_class  # not in the known taxonomy
