# Fixture: pipeline_config with an unknown top-level field.
# The strict schema should reject this with a ValidationError.

schema_version: 1

pathway_sources:
  MSIGDB:
    enabled: true
    propagating_predicates_g_to_pw:
      - inverse_pathway_associated_with_gene

gene_sabs:
  - HGNC

burden_control_exclusions: []

# Unknown top-level field; strict schema rejects.
unknown_top_level_field: "this should cause a validation error"
