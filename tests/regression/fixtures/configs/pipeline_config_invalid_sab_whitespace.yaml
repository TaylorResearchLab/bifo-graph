# Fixture: pipeline_config with a SAB key containing whitespace.
# The SAB validator should reject this.

schema_version: 1

pathway_sources:
  "MSIGDB BAD":  # SAB with embedded space; invalid
    enabled: true
    propagating_predicates_g_to_pw:
      - inverse_pathway_associated_with_gene

gene_sabs:
  - HGNC

burden_control_exclusions: []
