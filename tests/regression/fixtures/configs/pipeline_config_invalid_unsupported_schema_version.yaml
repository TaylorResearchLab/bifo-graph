# Fixture: pipeline_config with an unsupported schema_version.
# The schema_version validator should reject this with a clear error.

schema_version: 999  # not supported by this loader

pathway_sources:
  MSIGDB:
    enabled: true
    propagating_predicates_g_to_pw:
      - inverse_pathway_associated_with_gene

gene_sabs:
  - HGNC

burden_control_exclusions: []
