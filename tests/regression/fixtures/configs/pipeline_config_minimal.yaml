# Fixture: minimal valid pipeline_config.
# One enabled source, GO disabled, no policy filters.
# Exercises happy-path loader behavior.

schema_version: 1

pathway_sources:
  MSIGDB:
    enabled: true
    propagating_predicates_g_to_pw:
      - inverse_pathway_associated_with_gene
    kg_name_prefixes:
      - HALLMARK_

  GO:
    enabled: false
    propagating_predicates_g_to_pw:
      - gene_plays_role_in_process
    kg_name_prefixes: []

gene_sabs:
  - HGNC

burden_control_exclusions: []
